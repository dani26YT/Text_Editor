Imports System.IO

Module Module1
    Sub Main()
        Dim filePath As String = "C:\Windows\py.exe"

        If File.Exists(filePath) Then
            Console.WriteLine("Il file esiste.")
                Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        MessageBox.Show("Python è installato")
                    Dim exePath As String = "C:\Text Editor\Dani26.bat"
        Else
            Console.WriteLine("Il file non esiste.")
                            Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        MessageBox.Show("Python non è installato ora lo installerai con l'interfaccia grafica")
            Dim exePath As String = "C:\Text Editor\Programmi\Python.exe"
                Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        MessageBox.Show("Hello World")
        End If

        Console.ReadLine()
    End Sub
End Module